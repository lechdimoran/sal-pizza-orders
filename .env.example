# Environment Variables for Pizza Orders App
# Copy this file to .env and fill in your actual values

# API Configuration
REACT_APP_API_BASE_URL=https://sal-707-dl.onrender.com

# Optional: API key for non-authenticated endpoints (if needed)
# REACT_APP_API_KEY=your_api_key_here

# Note: Authentication is now handled via JWT tokens
# The app will obtain JWT tokens through the login process